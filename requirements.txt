# ============================================================================
# CORE DATA & ML DEPENDENCIES (Pinned Versions)
# ============================================================================
fredapi==0.5.2
yfinance==0.2.44
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
openpyxl==3.1.5
tensorflow==2.15.0
keras==2.15.0
xgboost==2.1.2
lightgbm==4.5.0
catboost==1.2.7
imbalanced-learn==0.12.4

# ============================================================================
# ADVANCED ML & OPTIMIZATION
# ============================================================================
optuna==4.1.0

# ============================================================================
# MODEL VERSIONING & MLOPS
# ============================================================================
mlflow==2.18.0

# ============================================================================
# MODEL INTERPRETABILITY
# ============================================================================
shap==0.46.0

# ============================================================================
# DATABASE
# ============================================================================
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# ============================================================================
# API & WEB FRAMEWORK
# ============================================================================
fastapi==0.115.5
uvicorn==0.32.1
pydantic==2.10.2

# ============================================================================
# DASHBOARD & VISUALIZATION
# ============================================================================
streamlit==1.40.1
plotly==5.24.1

# ============================================================================
# UTILITIES
# ============================================================================
requests==2.32.3
python-dotenv==1.0.1
beautifulsoup4==4.12.3
lxml==5.3.0
pyyaml==6.0.2
tqdm==4.67.0

# ============================================================================
# TESTING
# ============================================================================
pytest==8.3.3
pytest-cov==6.0.0
pytest-asyncio==0.24.0

# ============================================================================
# MONITORING
# ============================================================================
prometheus-client==0.21.0
sentry-sdk==2.18.0

# ============================================================================
# REMOVED DEPENDENCIES (Unused - Saved 30+ packages!)
# ============================================================================
# torch==2.1.2 - NOT USED (no PyTorch models in codebase)
# transformers==4.36.2 - NOT USED (no transformer models)
# dvc==3.58.0 - NOT USED (no DVC configuration)
# celery==5.4.0 - NOT USED (no Celery tasks)
# redis==5.2.0 - NOT USED (no Redis caching)
# alembic==1.14.0 - NOT USED (no database migrations)
# APScheduler==3.10.4 - NOT USED (no scheduled tasks)
# websockets==14.1 - NOT USED (FastAPI doesn't need it)
# altair==5.4.1 - NOT USED (using Plotly instead)
# lime==0.2.0.1 - NOT USED (using SHAP instead)
# black==24.10.0 - DEV ONLY (not needed for production)
# pylint==3.3.1 - DEV ONLY (not needed for production)
# mypy==1.13.0 - DEV ONLY (not needed for production)

